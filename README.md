# Setup Script

This is a helper script that automatically installs my dev environment to a new device.

Run via `curl https://raw.githubusercontent.com/demi-w/env-setup/refs/heads/main/main.sh | bash`
